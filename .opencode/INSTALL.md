# OpenCode Installation

Add to your `opencode.json`:

```json
{
  "plugin": [
    "worktree-compose@git+https://github.com/freedomprogramer/worktree-compose.git"
  ]
}
```

The plugin will be auto-cloned and skills will be available on next startup.
